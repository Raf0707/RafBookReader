package com.byteflipper.everbook.presentation.screens.start.data

import androidx.compose.runtime.Immutable

@Immutable
enum class StartNavigationScreen {
    LANGUAGE_FIRST,
    APPEARANCE_SECOND,
    PERMISSIONS_THIRD
}